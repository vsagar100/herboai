# Core framework
Flask==3.0.3

# Optional but strongly recommended for richer JSON and UTF-8 handling
itsdangerous>=2.1.2
Werkzeug>=3.0.1
Jinja2>=3.1.3

# For structured responses / utilities (standard but lightweight)
python-dotenv>=1.0.1

# (No external SQLite/ORM dependency — uses Python stdlib)
